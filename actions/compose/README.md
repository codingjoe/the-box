# Docker Compose

Update Docker compose plugin to the latest or specific version.

## Usage

```yaml
  - name: Update Docker Compose plugin
    uses: actions/compose@v2
    with:
      version: 2.0.0 # Optional, specify the version to install (default is latest)
```
